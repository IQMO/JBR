{
  "name": "@jabbr/shared",
  "version": "1.0.0",
  "description": "Shared types, validation schemas, and utilities for Jabbr trading bot platform",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "jest",
    "test:unit": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint \"src/**/*.ts\""
  },
  "dependencies": {
    "zod": "^3.25.67"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
